import * as React from 'react'
import { useState, useEffect } from 'react'

export const Button = () => {

    return (
        <button 
            type="submit"
            aria-label='Generate'
        >Generate</button>
    )
}